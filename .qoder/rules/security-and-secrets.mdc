---
alwaysApply: true
---
# 安全与密钥

- 禁止提交任何真实凭证、Token、Cookie、Access Key。
- 仅在本地使用 `backend/.env`；PR/CI 中通过环境变量注入。
- `config/agents.json` 提交前脱敏；必要时提供示例 [config/agents.example.json](mdc:config/agents.example.json)。
- 依赖与外部调用：设置超时、重试、熔断；记录审计日志。
- 对外接口返回语义化状态码与 `code` 字段，避免泄漏内部堆栈。
