---
globs: *.ts,*.tsx
---
# TypeScript 规范（前后端通用）

- 使用两空格缩进、单引号、显式导出接口与公共 API。
- 避免 `any` 与不安全断言；优先使用精确类型与泛型约束。
- 函数名用动词短语，变量名用名词短语，语义清晰。
- 早返回、处理边界情况，避免深层嵌套。
- 禁止无意义的 try/catch；错误需语义化信息与 cause。
- React 组件使用 PascalCase；Hooks 前缀 `use`，避免在循环中调用。
- 遵守路径别名 `@/...`（Vite 与 tsconfig-paths 已配置）。
