---
description: Playwright 端到端测试规范
---
# Playwright E2E 规范

- 目录： [tests/e2e](mdc:tests/e2e)；测试用例独立可并行，避免共享状态。
- 稳定性：使用 data-testid 定位；避免依赖动画/时间；适当超时与等待条件。
- 可溯源：保存 trace、视频与截图（失败时）；命名包含用例与场景。
- 环境：使用可控种子数据/夹具；外部依赖使用 mock/沙箱环境。
- 报告：输出 JUnit/HTML 报告；CI 中保留 artifacts 以便溯源。
