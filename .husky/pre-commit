#!/usr/bin/env sh

# Windows兼容的pre-commit hook
# 简化版本，只做基本检查

echo "✓ Pre-commit hook running..."

# 类型检查（仅警告，不阻止）
echo "Running type check..."
cd backend && pnpm run type-check 2>/dev/null || echo "⚠ Type check skipped (errors ignored)"

echo "✓ Pre-commit checks complete"
exit 0
